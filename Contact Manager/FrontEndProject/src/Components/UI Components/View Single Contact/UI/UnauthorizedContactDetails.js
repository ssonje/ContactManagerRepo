import React from "react";

/**
 * @Component
 * `UnauthorizedContactDetails` component provides the UI for viewing the unathorized contact details.
 */
const UnauthorizedContactDetails = () => {

    return (
        <div>
            Your not authorized to view others contact details
        </div>
    );
}

export default UnauthorizedContactDetails;
